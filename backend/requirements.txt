# FastAPI and ASGI server
# Upper-Cap (TF-425): FastAPI 0.137.0 führt _IncludedRouter ein und stellt
# include_router so um, dass Sub-Routes nicht mehr flach in app.routes liegen
# (bricht route_paths = [r.path for r in app.routes] in den Tests). 0.136.x ist
# OK. Cap entfernen, sobald Tests/App auf das neue Routing-Modell migriert sind.
fastapi>=0.117.1,<0.137
uvicorn[standard]==0.24.0

# Database
sqlalchemy>=2.0.43
alembic>=1.17.0
psycopg2-binary>=2.9.10

# Redis
redis==5.0.1

# S3 Storage
boto3>=1.28.0

# Payment
stripe>=14.2.0


# Message Queue & Task Processing
celery[redis]==5.4.0                    # Celery Task Queue with Redis Backend
flower==2.0.1                           # Celery Monitoring Dashboard
kombu==5.4.0                            # Messaging Library for Celery
amqp==5.2.0                             # AMQP Protocol Implementation

# HTTP client for Claude API
httpx>=0.28.1

# SubscribeFlow SDK (Newsletter Subscription)
subscribeflow>=1.0.5

# Internationalization
python-i18n[YAML]>=0.3.9            # i18n Translation Support

# Configuration
pyyaml>=6.0.2                       # YAML Configuration
# Template Engine (required for Premium features)
Jinja2>=3.1.0

# Environment and configuration
python-dotenv==1.0.0
pydantic==2.13.4                    # pydantic-ai 1.x floor (ag-ui-protocol dep; war 2.10.3)
pydantic-settings==2.14.1          # bumped mit pydantic (war 2.7.0)
pydantic-ai==1.0.15                 # PydanticAI for ChatBot (Gateway-Pfad: OpenAIChatModel + OpenAIProvider)
anthropic==0.79.0                   # Exakter Pin: pydantic-ai 1.0.15 braucht UserLocation in beta_web_search_tool_20250305_param (ab 0.80.0 entfernt); 0.79.0 ist letzte Version mit diesem Symbol + vollem cache_control-Support
email-validator>=2.3.0              # Email validation for Pydantic EmailStr

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.32            # pydantic-ai 1.x / mcp floor (war 0.0.6)
authlib==1.3.0                      # OAuth2 Client Library
itsdangerous==2.1.2                 # Secure Token Generation for OAuth State

# Error Tracking & Monitoring
sentry-sdk[fastapi]==2.42.1         # Sentry SDK with FastAPI Integration

# CORS (built into FastAPI)
# No additional package needed

# Development & Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0                   # Coverage Reports
pytest-mock==3.12.0                 # Mock Utilities
respx==0.23.1                       # httpx mocking for Moodle-API-Driver tests (TF-336)
ruff==0.15.11                       # Linter + Formatter (pinned to prevent CI/local drift)

# Document Processing & Upload
aiofiles==23.2.1                    # Async File Operations
python-magic==0.4.27                # File Type Detection

# PyMuPDF - Fast PDF Processing (Default)
PyMuPDF==1.24.14                    # Fast PDF Processing with fitz

# Document Processing
python-docx==1.1.2                  # DOCX Processing
markdown==3.5.1                     # Markdown Processing

# Legacy/Alternative Processors (Optional)
pypdf>=6.1.0                        # PDF Processing Fallback

# PDF Generation (TF-335 Notenexport)
reportlab>=4.2.0                    # PDF Generation für GradePdfExporter
# docling==2.23.0                   # IBM Docling (deprecated - too slow)
# docling-core==2.48.4              # Docling Core (deprecated)
# docling-ibm-models==3.9.1         # IBM Models (deprecated)

# Vector Database & Embeddings (Premium Features)
qdrant-client==1.16.2             # Qdrant Vector Database Client (Premium)
openai==1.109.1                   # OpenAI API for Embeddings (Premium; war 1.59.5)
# sentence-transformers==2.7.0      # Sentence Embeddings (Premium)

numpy>=2.1.0                        # Numerische Operations (für Embeddings)
